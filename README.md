# a simple discord utility bot

A little discord bot I wrote back in 2023, using my own discord API libraries. Don't mind the hardcoded API token and snowflake IDs. They are (probably) expired by now...

Features:

- Autoroles
- Button roles
- Embed generator
